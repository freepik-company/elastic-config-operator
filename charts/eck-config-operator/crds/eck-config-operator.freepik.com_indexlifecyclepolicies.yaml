---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
    helm.sh/resource-policy: keep
  name: indexlifecyclepolicies.eck-config-operator.freepik.com
spec:
  group: eck-config-operator.freepik.com
  names:
    kind: IndexLifecyclePolicy
    listKind: IndexLifecyclePolicyList
    plural: indexlifecyclepolicies
    singular: indexlifecyclepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    - jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IndexLifecyclePolicy is the Schema for the indexlifecyclepolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IndexLifecyclePolicy
            properties:
              resourceSelector:
                description: ResourceSelector defines how to select and connect to
                  an Elasticsearch cluster
                properties:
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      CA certificate
                    properties:
                      key:
                        description: Key in the secret to select
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret (optional, defaults to
                          the same namespace as the resource)
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  endpoint:
                    description: |-
                      Manual configuration (optional) - if provided, these values override ECK automatic discovery
                      Endpoint is the Elasticsearch URL (e.g., https://my-elasticsearch.example.com:9200)
                    type: string
                  name:
                    description: Name of the Elasticsearch resource (ECK cluster name)
                    type: string
                  namespace:
                    description: Namespace of the Elasticsearch resource (defaults
                      to the same namespace as this resource)
                    type: string
                  passwordSecretRef:
                    description: PasswordSecretRef references a Secret containing
                      the password
                    properties:
                      key:
                        description: Key in the secret to select
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret (optional, defaults to
                          the same namespace as the resource)
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  username:
                    description: Username for Elasticsearch authentication
                    type: string
                required:
                - name
                type: object
              resources:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
              syncInterval:
                default: 10s
                description: SyncInterval defines the interval for reconciliation
                  (e.g., "30s", "5m"). Defaults to 10s.
                type: string
            required:
            - resourceSelector
            - resources
            type: object
          status:
            description: status defines the observed state of IndexLifecyclePolicy
            properties:
              appliedResources:
                description: AppliedResources is a list of resource names that have
                  been successfully applied to Elasticsearch
                items:
                  type: string
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the IndexLifecyclePolicy resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  synchronization with Elasticsearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              phase:
                description: |-
                  Phase represents the current phase of the IndexLifecyclePolicy
                  Possible values: Pending, Syncing, Ready, Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
