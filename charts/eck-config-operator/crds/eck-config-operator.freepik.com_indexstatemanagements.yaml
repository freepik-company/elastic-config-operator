---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: indexstatemanagements.eck-config-operator.freepik.com
spec:
  group: eck-config-operator.freepik.com
  names:
    kind: IndexStateManagement
    listKind: IndexStateManagementList
    plural: indexstatemanagements
    singular: indexstatemanagement
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current phase of the IndexStateManagement
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Target cluster
      jsonPath: .status.targetCluster
      name: Cluster
      type: string
    - description: Detailed status message
      jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    - description: Last successful synchronization time
      jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IndexStateManagement is the Schema for the indexstatemanagements API
          This resource is specifically for OpenSearch clusters (ISM API)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IndexStateManagement
            properties:
              resourceSelector:
                description: ResourceSelector specifies the target OpenSearch cluster
                  for ISM policies
                properties:
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      CA certificate
                    properties:
                      key:
                        description: Key in the secret to select
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret (optional, defaults to
                          the same namespace as the resource)
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  clusterType:
                    description: |-
                      ClusterType specifies the type of cluster: "elasticsearch" or "opensearch"
                      If not specified, the operator will automatically detect the cluster type
                    enum:
                    - elasticsearch
                    - opensearch
                    type: string
                  endpoint:
                    description: |-
                      Manual configuration (optional) - if provided, these values override ECK automatic discovery
                      Endpoint is the Elasticsearch URL (e.g., https://my-elasticsearch.example.com:9200)
                    type: string
                  name:
                    description: Name of the Elasticsearch resource (ECK cluster name)
                    type: string
                  namespace:
                    description: Namespace of the Elasticsearch resource (defaults
                      to the same namespace as this resource)
                    type: string
                  passwordSecretRef:
                    description: PasswordSecretRef references a Secret containing
                      the password
                    properties:
                      key:
                        description: Key in the secret to select
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret (optional, defaults to
                          the same namespace as the resource)
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  username:
                    description: Username for Elasticsearch authentication
                    type: string
                required:
                - name
                type: object
              resources:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Resources contains the ISM policies to apply, keyed by policy name
                  Each key represents a policy name, the value is the policy definition
                type: object
              syncInterval:
                description: |-
                  SyncInterval defines how often the operator will reconcile this resource (default: 10s)
                  Examples: "30s", "5m", "1h"
                type: string
            required:
            - resourceSelector
            - resources
            type: object
          status:
            description: status defines the observed state of IndexStateManagement
            properties:
              appliedResources:
                description: |-
                  AppliedResources lists the names of the policies that were successfully applied to OpenSearch.
                  This is used to track which policies need to be deleted if they are removed from the spec.
                items:
                  type: string
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the IndexStateManagement resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime records the last time the resource was successfully
                  synchronized with OpenSearch.
                format: date-time
                type: string
              message:
                description: Message provides a human-readable message about the current
                  status.
                type: string
              phase:
                description: |-
                  Phase indicates the current phase of the IndexStateManagement.
                  It can be "Pending", "Syncing", "Ready", or "Error".
                type: string
              targetCluster:
                description: |-
                  TargetCluster is the namespace/name of the target OpenSearch cluster
                  Format: "namespace/name"
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
